name = "arc_between_points"
code = """
```rust
use itertools::Itertools;
use ranim::{
    anims::fading::FadingAnim,
    color,
    color::HueDirection,
    glam::{DMat2, dvec2},
    items::vitem::geometry::ArcBetweenPoints,
    prelude::*,
    utils::rate_functions::smooth,
};

#[scene]
#[wasm_demo_doc]
#[output(dir = "./output/arc_between_points")]
fn arc_between_points(r: &mut RanimScene) {
    let center = dvec2(0.0, 0.0);

    let start_color = color::color("#FF8080FF");
    let end_color = color::color("#58C4DDFF");
    let ntan = 16;
    let nrad = 5;

    let mut arcs = (0..nrad)
        .map(|i| {
            let radius = 6.0 * (i + 1) as f64 / nrad as f64;
            let width = 0.12 * ((nrad - i) as f64 / nrad as f64).powi(2);
            let angle = std::f64::consts::PI * 7.0 / 4.0 * (i + 1) as f64 / nrad as f64;
            (radius, width, angle)
        })
        .cartesian_product(0..ntan)
        .map(|((rad, width, angle), j)| {
            let color = start_color.lerp(
                end_color,
                j as f32 / (ntan - 1) as f32,
                HueDirection::Increasing,
            );
            let vec = DMat2::from_angle(std::f64::consts::PI * 2.0 / ntan as f64 * j as f64)
                * dvec2(rad, 0.0);
            ArcBetweenPoints::new(center.extend(0.0), (center + vec).extend(0.0), angle).with(
                |arc| {
                    arc.stroke_width = width as f32;
                    arc.set_stroke_color(color);
                },
            )
        })
        .collect::<Vec<_>>();
    let total_secs = 3.0;
    let content = arcs
        .iter_mut()
        .map(|arc| arc.fade_in().with_rate_func(smooth))
        .into_lagged(0.2)
        .with_duration(total_secs);

    r.play(CameraFrame::default().show().with_duration(total_secs));
    r.play(content);

    r.insert_time_mark(total_secs, TimeMark::Capture("preview.png".to_string()));
}
```"""
hash = "313ba4e302839c8b1e82f3c246a523e44aa11e7f"
preview_imgs = ["https://azurice-shadow.tos-cn-beijing.volces.com/ranim/objects/sha256/4f/06834d59bf26b6fa2e6bdea34ea58e1c9367e3e80d7ac7d65672524f3b925e"]
wasm = true

[[output_files]]
url = "https://azurice-shadow.tos-cn-beijing.volces.com/ranim/objects/sha256/44/a0be077ec4bc882ded44ba8fb2c6baaa227fc9bc4f847c986c22b22b013a71"
kind = "video/mp4"

[[output_files]]
url = "https://azurice-shadow.tos-cn-beijing.volces.com/ranim/objects/sha256/4f/06834d59bf26b6fa2e6bdea34ea58e1c9367e3e80d7ac7d65672524f3b925e"
kind = "image/png"
