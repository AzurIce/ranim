name = "getting_started_1"
code = """
```rust,linenos
use ranim::{\r
    animation::fading::{FadingAnim, FadingAnimSchedule},\r
    color::palettes::manim,\r
    items::vitem::Square,\r
    prelude::*,\r
};\r
\r
#[timeline]\r
fn getting_started_1(ranim: Ranim) {\r
    let Ranim(timeline, mut _camera) = ranim;\r
\r
    let mut square = Square(300.0).build();\r
    square.set_color(manim::BLUE_C);\r
\r
    let mut square = timeline.insert(square);\r
    timeline.play(square.fade_in().chain(|data| data.fade_out()));\r
}\r
\r
fn main() {\r
    render_timeline!(getting_started_1);\r
}\r

```"""
hash = "9d9809846e8b337f99132315bb006f5385617b2a"
output_files = ["/examples/getting_started_1/output.mp4"]
