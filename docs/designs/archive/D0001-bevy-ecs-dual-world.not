= D0001: bevy_ecs 双 World 架构

（已废止）本设计由 [[super::super::D0002-incremental-bevy-ecs]] 取代。当前方向把 ECS 限制为渲染原语的内部存储，用户级 item 不再进入任何 World，因而本篇的逻辑 World、跨 dylib 分发和声明式 item 注册都不再是 ranim 的现行约束。

本篇保留的是一套曾经成立的判断：如果 ranim 要把用户级 item 作为可被外部查询、内省和扩展的运行时对象，那么 ECS 不能只替换一个 store；它需要同时定义用户语义、渲染语义和动态库边界如何相接。它也记录了为什么这种能力有代价，以及什么需求出现时值得重新考虑它。

== 要解决的不是一次分桶

早期的 `CoreItemStore` 是手写的封闭集合：每种渲染原语各有一对 `Vec`，每帧把 `CoreItem` 枚举 `match` 到对应 bucket。它对既有原语足够直接，但新增一种原语需要在枚举、store、buffer、pipeline 和 render graph 等多处重复接线。漏掉一处，结果不是编译失败就是静默少画。

如果问题仅是减少这些接线，内部 World 已经足够；这正是 D0002 的选择。D0001 处理的是更强的问题：下游 crate 定义的 item 能否成为场景中可识别、可查询的对象，并在不修改核心分桶逻辑的情况下接入渲染。这个问题要求区分两种本来就不应混在一起的语义。

== 两种 World，两种语言

```text
动画树 (AnimStack)
│  eval_at_sec(t)：纯函数求值
▼
逻辑 World：实体 = 动画单元的输出，组件 = 用户级 item
│  extract：把用户语义降级为渲染原语
▼
渲染 World：每种 CoreItem 一个 bucket，另有相机状态
│  按类型合并 storage buffer，instanced draw
▼
GPU
```

逻辑 World 面向作者的表达：`SvgItem`、`TypstDoc`、`VItem` 和未来的外部 item 都可以是组件。渲染 World 面向 GPU 能画什么：它只认识渲染原语及其合并方式。两者之间的 extract 是有方向的降级；一个高层 item 可以展开成多个原语，而渲染原语不应反向承担动画或文档语义。

这里同样划出 ECS 不做的两件事。动画求值不进入 ECS：父子动画、时间区间和 rate function 在纯函数求值阶段完成，World 保存的是该帧结果。渲染遍历也不按实体分发 draw call：draw 仍按原语类型合批。ECS 负责的是中间的类型化存储、查询和调度，而不是取代动画模型或渲染管线。

== 为什么不共用一个 World

把用户 item 与渲染原语放在同一个 World，会让渲染侧在每增加一种用户类型时都必须认识、跳过或处理它。用户 API 与 GPU 管线于是被同一套组件集合绑住，演进速度不同的两层被迫同步。

分成两个 World 后，依赖只从逻辑侧流向渲染侧：extract 同时读取逻辑 World 并写入渲染 World，渲染 World 对用户类型没有知识。这个边界也解释了实体的意义不同：逻辑 World 的实体可代表稳定的动画单元输出；渲染 World 的 bucket 是每帧参与合批的数据，不需要伪装成逐原语长期存在的实体。

== 逻辑实体保存终值，而非动画关系

动画树 seal 后形成动画单元序列。一项单元一帧可以产生多个 item，因此稳定身份可写成 `(animation_id, part)`：`part` 是该帧输出中的出现序号。这个身份服务于跨帧更新和变更检测，不改变动画树本身的语义。

组件中保存的是叠加后的终值，不是父子动画关系。若把关系树也建成 ECS 实体，渲染侧就必须理解动画语义才能知道应画什么，纯函数求值会被拆成两个互相依赖的阶段。保持“求值产生终值，World 保存终值”使动画逻辑能独立测试，也使 extract 总是面对可直接降级的数据。

为避免要求每个 item crate 依赖 `bevy_ecs`，可由 core 定义一次性的泛型包装，例如 `Evaluated<T>(T)`，并让包装实现组件。于是注册和查询仍以 `T` 为单位，用户 item 本身不必 `derive(Component)`；ECS 依赖局限在拥有 World 的包内。

== 渲染 World 是 bucket，不是场景镜像

渲染器把同类原语合并成一个 storage buffer，再以一次 instanced draw 提交。因此渲染 World 中自然的单位是每类原语一个类型化 bucket，而不是每个 `CoreItem` 一个实体。相机同理：单相机模型下，`CameraFrame` 是资源而非只有一个实例的组件。

这排除了把逻辑实体一一同步为渲染实体的 retained 模型。那种模型适合实体身份本身被 GPU 状态或渲染查询消费的游戏场景；在这里，几何每帧重新生成，最终仍按类型合批，维护映射只增加 reconciliation 与清理复杂度。内容相同则跳过上传的收益应在 buffer 层通过比较内容取得，而不依赖渲染实体身份。

== Extract 是语义降级的边界

extract schedule 读取逻辑 World、写入渲染 World。每个用户级 item 有一个类型化 system：查询 `Evaluated<T>`，调用既有的 `Extract<Target = CoreItem>` 语义，并把结果写入相应 bucket。一个 item 展开成多个原语是正常情形；bucket 不要求输入与输出一一对应。

```rust
fn extract_typed<T>(items: Query<&Evaluated<T>>, mut out: ResMut<CoreItemBuckets>)
where
    T: Extract<Target = CoreItem>,
{
    for item in &items {
        item.0.extract_into(out.core_items());
    }
}
```

这个形态让“实现提取”与“接入提取”成为同一件事。它也是 D0001 的开放性来源：下游作者定义类型及其降级规则后，系统应能发现并装配这一提取器，而不要求作者回到核心 crate 修改一个封闭枚举。

== dylib 改变了类型分发的含义

ranim 的场景以 `cdylib` 加载。宿主和 dylib 可由相同源码、相同工具链构建，却是两个二进制；两边静态链接的 crate 实例不同。现有模型已经依赖二者的 Rust 类型布局、vtable 与 allocator 行为一致，这也是加载场景的前提。D0001 不试图把这个模型变成 C ABI，而是在该前提下避免再误用类型身份。

`TypeId` 的有效域是一个二进制。dylib 一侧登记的组件，在宿主以同一源级类型查询时，二者的 `TypeId` 仍可能不同，不能用作跨边界的分发键。跨边界需要稳定、可传递的名字，例如 `std::any::type_name::<T>()`；在各自的 World 内部仍可使用 `TypeId` 或 `ComponentId` 提高查询效率。

类型名不是数学上全局唯一的名字：不同 crate 可以构造相同路径。这是此模型接受的风险，适用于同一源码树构建的场景。若真实生态需要显式消歧，注册载荷应增加可覆盖的类型路径，而不是重新把边界键换回 `TypeId`。

同时，宿主和 dylib 的版本漂移会把既有布局假设变成 UB，而不是普通错误。因此加载握手应校验 ranim 版本或 git hash；该校验不是类型注册的便利功能，而是已有二进制边界假设的保险。

== 声明式注册让类型知识留在生产方

宿主不能为它未编译进来的 `TextItem` 等类型单态化提取 system，特别是 feature 集不完全相同时。因此“宿主手动列出所有类型”不是可靠的装配方式；类型知识必须留在实际产出 item 的 dylib。

可行的模型有三层：

- `#[item]` 标注类型，并在该类型所在二进制中生成一项注册记录；记录包含类型名和已单态化的提取函数。
- dylib 导出一个枚举符号，供宿主在加载后取得该二进制收集到的记录。二进制本地的 inventory 或 link-time collection 只负责收集，不能被误认为宿主可见的全局表。
- 宿主以自己的代码为每项记录构造 schedule system；system 按类型名找到已记录的组件，再调用 dylib 提供的提取函数。

穿过边界的是平凡函数指针、稳定名字和既有共享布局的数据，不让泛型或 ECS 类型直接穿越。作者侧的体验因此保持为“声明一个 item，提供它的降级语义”，而不是在多个宿主装配点手写注册。

`wasm` 是否能可靠保留这类链接期静态项、以及泛型 item 怎样显式登记，都是该注册模型必须面对的边界。它们不是理由去取消生产方注册原则：前者可改用同一载荷的惰性收集，后者可在具体单态化处登记。

== 什么不属于这套设计

外部 item 的开放，不等于外部渲染原语自动开放。普通 item 可以降级为内建 `CoreItem`；真正新增原语还涉及 GPU 资源、pipeline 与 render graph 依赖，必须以显式注册和依赖声明接入。保留 `CoreItem` 的封闭集合，恰好使共享主循环与内建原语的契约保持明确。

同样，D0001 不引入 `bevy_app` 或 `bevy_render`。需要的是 `bevy_ecs` 的 World、query 与 schedule 模型；渲染管线、OIT 与 graph 仍是 ranim 自己的边界。ECS 的编译时间和二进制体积成本、以及它要求的 Rust 版本，只有在开放 item 体系确实有消费者时才值得支付。

== 被取代的原因

D0001 把 editor 内省、跨边界查询和外部渲染原语视为值得预先支持的未来能力。D0002 的结论是这些能力尚无真实消费者，而它们带来的双 World 和 dylib 注册复杂度不能仅凭设想合理化。因而 D0001 不再约束当前实现；当且仅当这些开放性需求成为实际问题时，本篇仍可作为重新评估的候选模型。
