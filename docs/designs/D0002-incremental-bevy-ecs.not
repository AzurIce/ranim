= D0002: 渐进式引入 bevy_ecs

这份设计回答一个问题：ranim 应如何引入 `bevy_ecs`，而不改变它向场景作者承诺的模型？答案是把 ECS 限制为渲染器内部的存储与组织机制。它取代 [[super::archive::D0001-bevy-ecs-dual-world]]：用户级 item 不进入 World，不跨 dylib 注册 ECS 类型，也不把 ECS 暴露为下游扩展接口。

这里的“渐进”描述的是架构形态，而不是交付阶段：无论内部使用何种 World、查询或 system，场景作者看见的数据流、类型边界和 dylib 载荷都保持同一语义。ECS 可以逐处吸收确有收益的内部职责，却不能成为公开模型的一部分。

== 只有中间的容器改变

```text
动画树 (AnimStack)
│  eval_at_sec(t)：纯函数求值
▼
((animation_id, part), DynItem)：类型擦除的 item
│  vtable extract_into：降级为若干 CoreItem
▼
内部 World：VItem / MeshItem / CameraFrame 等内部类型
│  按原语类型合并 storage buffer，instanced draw
▼
GPU
```

动画求值仍然产生 `DynItem`，提取仍通过其 vtable 运行，渲染仍消费 `CoreItem` 并按原语合批。与原有实现相比，改变的是第三跳的容器：手写的“封闭枚举 match 加每原语一对 `Vec`”由 `bevy_ecs::World` 的类型化存储取代。

因此 World 不表示场景，也不表示用户可查询的对象集合。它只是 ranim 在一帧渲染准备过程中保存内部原语与状态的方式，正如 `CoreItemStore` 曾经是实现细节。

== 公共边界不因 ECS 移动

三条规则定义“ECS 纯内部化”的含义。

首先，用户级 item 绝不成为 ECS 组件。World 中只有 ranim 自己定义和拥有的渲染原语及内部状态；用户类型与 World 之间始终隔着 `DynItem` 的 vtable 提取。World 因而不需要，也不能认识下游 crate 的 item。

其次，ECS 类型不出现在公开 API 签名。`ranim-items` 不依赖 `bevy_ecs`，item、动画和场景接口不要求 `World`、`Component` 或 `Query`。调用者选择或不选择 ranim 的 ECS 实现，都不改变其 item 的定义和提取语义。

最后，dylib 边界上的载荷不变：vtable、按值传递的 `CoreItem` 和既有的平凡函数指针仍是边界协议。ECS 不增加新的跨二进制类型识别或 ABI 假设；same source + same toolchain 的既有前提也不因它扩大。

这些规则共同避免一种看似方便的滑坡：先让 item 进入 World，再为它们补查询、注册和跨 dylib 类型分发。那会重新落入 D0001 的模型，却没有先证明其复杂度有消费者承担。

== ECS 买到的是内部演进空间

内部原语以组件形式存储后，组件类型自身就是存储与查询的单位。新增一种内建原语不必再修改同一个封闭 store 的字段和匹配分支；渲染准备逻辑可以按所需数据声明查询，并在适合时利用 system 的组织与调度能力。

`Changed<T>` 也为 CPU 侧跳过未变原语的打包留下了可能性。它与 GPU 上传前比较 buffer 内容是两个层次：前者判断是否需要重新准备数据，后者在最后一层避免上传相同字节。buffer 比较已经能独立保证静态帧不重复上传；ECS 不应被误述为这项上传优化的来源。

这些收益只属于核心实现。渲染原语集合对下游仍然封闭，外部 item 仍须降级为内建原语；内部 World 是否采用额外的资源、query 或 system，是维护者可以改变的实现组织，而非场景 API 的设计承诺。

== 身份只服务内部变更语义

World 中的实体可用来保留内部原语的身份，从而支持变更检测或 reconciliation，但这种身份没有公开语义。它描述的是“当前帧的内部 `CoreItem` 集合”如何与先前状态对应，而不是场景作者可引用的对象。

一旦内部保留实体，key 必须来自数据本身的语义身份，例如动画单元标识或文档标识。不能用 `Vec` 下标等位置作为 key：删除或重排会让一个实体静默继承另一项的身份，使变更检测把不同内容误认为同一对象。若没有可信的语义 key，正确选择是将该数据视为当前帧集合，而不是伪造稳定身份。

== 明确不提供的能力

纯内部化意味着有些能力暂不成立，这不是遗漏。

- 外部 item 不能被 ECS 查询，也不能使用 ECS 的逐 item 变更检测；它们继续全量执行 vtable 提取，内部 buffer 层负责去重。
- 外部 crate 不能注册新的渲染原语。需要体渲染或场可视化等专用原语时，不能假装它只是普通 `Extract` 的一个变体。
- editor 的字段级内省不从 ECS 获得。组件存储不会自动产生字段描述，内省需要反射或按类型注册的独立设计。

这些限制保留了一个清晰的责任分界：item 扩展的是“如何降级为内建原语”，而渲染原语、GPU 资源和编辑器元数据各自需要有明确的所有者与模型。

当真实需求要求跨边界查询或内省场景 item、外部原语必须接入渲染管线，或内建原语的增长再次使内部渲染组织成为负担时，应重新评估 [[super::archive::D0001-bevy-ecs-dual-world]]。D0001 的双 World、类型名分发和声明式注册是那类需求的候选答案，不是当前设计的隐含待办事项。

== 依赖边界与代价

该设计只引入 `bevy_ecs`，不引入 `bevy_app` 或 `bevy_render`；关闭默认 feature，尤其不携带 `bevy_reflect`。依赖被约束在拥有内部 World 的 core 与渲染侧包内，不能渗入 item 或场景 API。

编译时间、二进制体积和 Rust 版本要求是为内部可维护性支付的真实成本。它们之所以合理，不是因为 ECS 将成为通用框架，而是因为 ranim 的内建渲染原语与准备逻辑已经需要类型化存储、查询和可演进的组织方式；若这些收益消失，依赖本身也应被重新审视。
